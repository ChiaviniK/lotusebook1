import React from 'react';

const ApiBuilder = () => {
  return (
    <div style={{ padding: '2rem', textAlign: 'center', backgroundColor: 'var(--color-surface)', borderRadius: '8px', border: '1px solid var(--color-border)' }}>
      <h2>Construtor de API (Em Construção)</h2>
      <p>Monte o endpoint e os parâmetros para formar um GET real ao Banco Mundial ou IBGE.</p>
    </div>
  );
};

export default ApiBuilder;
